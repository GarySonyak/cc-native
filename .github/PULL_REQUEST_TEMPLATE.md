## Summary

What this PR changes and why.

## Linked issue

Closes #

## Tests

- [ ] `make test` passes locally (14/14 fixtures).
- [ ] If this PR touches `hooks/cc-native-verify.py`, a fixture under `tests/fixtures/.claude/` was added that exercises the new behavior (one passing, one failing as appropriate).
- [ ] If this PR touches a reference page under `skills/feature-guide/references/`, the change reflects current `code.claude.com` documentation.

## Manual repro

How a reviewer can see the change end-to-end.
