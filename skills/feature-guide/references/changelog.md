# Commands & Changelog (recent versions)

## Key Commands (additions since last review)

- `/diff` -- interactive diff viewer for uncommitted changes + per-turn diffs
- `/insights` -- report on session patterns and friction points
- `/security-review` -- analyze pending branch changes for vulnerabilities
- `/sandbox` -- toggle sandbox mode (supported platforms only)
- `/setup-bedrock` -- Bedrock setup wizard (interactive auth/region/model config; visible when `CLAUDE_CODE_USE_BEDROCK=1`) (v2.1.92)
- `/setup-vertex` -- Google Vertex AI setup wizard (auth/project/region/model; visible when `CLAUDE_CODE_USE_VERTEX=1`) (v2.1.98)
- `/team-onboarding` -- generate teammate ramp-up guide from local Claude Code usage (v2.1.101)
- `/autofix-pr [prompt]` -- spawn cloud session to watch PR, push fixes for CI failures/review comments
- `/commit-push-pr` -- stage, commit, push, and open a PR in one flow; `git push` to the repo's configured push remote is auto-allowed without a separate permission prompt (v2.1.206, previously undocumented in this file)
- `/doctor` -- full setup checkup: diagnoses **and can fix** reported issues (press `f`); alias `/checkup`. (v2.1.205 expanded scope + alias; previously diagnosis-only)
- `/schedule [description]` -- create/manage Cloud scheduled tasks (1hr minimum, survives restarts)
- `/ultraplan <prompt>` -- draft plan in browser, execute remotely or return to terminal
- `/tasks` -- list and manage background tasks (alias: `/bashes`)
- `/remote-control` -- make session available for remote control from claude.ai (alias: `/rc`)
- `/plugin` -- manage Claude Code plugins
- `/skills` -- list available skills
- `/tui` -- toggle flicker-free fullscreen rendering mode (v2.1.110)
- `/ultrareview` -- parallel cloud-based multi-agent code review (v2.1.111); `claude ultrareview [target]` non-interactive (v2.1.120). Preferred invocation is now `/code-review ultra`; `/ultrareview [PR]` remains as an alias.
- `/fewer-permission-prompts` -- [Skill] scan transcripts for common read-only Bash/MCP calls and add allowlist to project settings.json (renamed from /less-permission-prompts, v2.1.114)
- `/usage` consolidates `/cost`+`/stats` (v2.1.118)
- `/heapdump` -- write JS heap snapshot + memory breakdown to `~/Desktop` (or home dir on Linux) for diagnosing high memory usage
- `/teleport` (alias `/tp`) -- pull a Claude Code on the web session into this terminal (fetches branch + conversation); requires claude.ai subscription
- `/goal` -- set completion condition; Claude works across turns until goal is met; use for non-interactive autonomous tasks (v2.1.139)
- `/background` -- detach current session as background agent (frees terminal); visible in Agent View (`claude agents`) (v2.1.139)
- `/stop` -- stop the current background session (only available while attached to a background session); transcript and worktree are kept. `/exit` or `←` to detach without stopping.
- `/reload-skills` -- re-scan skill directories for changes without restarting session (v2.1.152). Cf. `/reload-plugins` for plugins.
- `/deep-research [question]` -- `[Workflow]` fan out web searches, fetch and cross-check sources, synthesize a cited report. (v2.1.154)
- `/effort` now confirms when your chosen effort level is saved as the new default. (v2.1.162)
- `/plugin list [--enabled|--disabled]` -- list installed plugins, optionally filtered to only enabled or only disabled. (v2.1.163)
- `/cd <path>` -- move current session to a new working directory without breaking the prompt cache. (v2.1.169)
- `/advisor [model|off]` -- set or clear advisor model (stronger model Claude consults at key decisions); `advisorModel` setting for persistent default; `--advisor <model>` flag per-session. Experimental, Anthropic API only (not Bedrock/Vertex/Foundry). (v2.1.98+, Fable 5 in v2.1.170)
- `/workflows` -- open workflow progress view to watch, pause, resume, or save running and completed workflows. (v2.1.154)
- `/review [PR]` -- read-only review of a GitHub PR by number (lists open PRs if omitted); fast single-pass review. Distinct from `/code-review` (reviews local diff, can `--fix`) and `/code-review ultra`/`/ultrareview` (cloud multi-agent). Reverted to fast single-pass in v2.1.202 after an interim change. Previously undocumented in this file. (v2.1.202)
- `/design-login` -- authorize design-system access for `/design-sync` with your claude.ai account. (v2.1.196)
- `/design-sync [hint]` -- **[Skill]** convert your repo's React design system and upload it to Claude Design (claude.ai/design) so generated designs use your real components; Anthropic API only (not Bedrock/Vertex/Foundry). (v2.1.196)
- `ultracode` effort level -- combines xhigh reasoning with automatic workflow orchestration via Workflow tool; set with `/effort ultracode`. (v2.1.154)

## Version notes

- **v2.1.207** (2026-07-11): Auto mode on Bedrock, Vertex AI, and Foundry no longer requires `CLAUDE_CODE_ENABLE_AUTO_MODE=1` opt-in — enabled by default; disable org-wide with the existing `permissions.disableAutoMode: "disable"` managed setting (see modes-and-permissions.md; doc lag — live permission-modes page hadn't caught up as of this run). Default model on Bedrock/Vertex/Claude Platform on AWS changed to Claude Opus 4.8. Security: remote managed settings no longer silently recorded as consented without showing the security dialog; added shell-injection checks for plugin hooks. Bug fixes: terminal freezing with very long lists/tables/code blocks, spurious prompt-injection warnings, auto-updater overwriting custom `~/.local/bin/claude` launcher scripts, transcript jump after streaming completes, background-session/Remote Control fixes. (v2.1.207)
- **v2.1.206** (2026-07-09): `EnterWorktree` now asks for confirmation before entering a git worktree outside `.claude/worktrees/` (see tools-and-scheduling.md). `/commit-push-pr` — previously-undocumented bundled skill that stages, commits, pushes, and opens a PR — now auto-allows the `git push` step to the repo's configured push remote without a separate prompt. Gateway: `/login` now supports Anthropic-operated public gateway endpoints. Trivial: `/cd` gained directory path suggestions (matches `/add-dir`); `/doctor` proposes trimming checked-in `CLAUDE.md` files; background agents auto-upgrade in the background after a Claude Code update instead of needing a restart; `/code-review` finding quality improved on claude-opus-4-8; bug fixes (login expiration, MCP timeouts, model picker display). (v2.1.206)
- **v2.1.205** (2026-07-08): New auto mode default block — tampering with session transcript/conversation-log files (see modes-and-permissions.md). `/doctor` is now a full setup checkup that can diagnose **and fix** issues (not diagnosis-only); `/checkup` added as an alias. Reserved "Claude Browser" MCP server name (with "Claude Preview") ahead of a Desktop pane rename — user servers can no longer register under either name. Auto mode now asks before running `rm -rf` on a variable it can't resolve from context, instead of allowing or blindly blocking. Agent view: rows show a colored state word + classifier-written headline instead of raw tool-call text; sessions that touch an existing PR now link it; background-task notifications explicitly state no human input occurred (prevents acting on fabricated in-transcript approvals). ~20 other bug fixes (Windows worktree removal via NTFS junctions, `--json-schema` invalid-schema handling, background-agent status flicker, `claude attach` mid-upgrade race, PR-linking for large Bash output, plugin LSP init failures, updater memory ~400MB lower). (v2.1.205)
- **v2.1.204** (2026-07-08): Bug fix only — hook events no longer fail to stream during `SessionStart` hooks in headless sessions, which could cause remote workers to be idle-reaped mid-hook. (v2.1.204)
- **v2.1.203** (2026-07-07): MCP `roots/list` now also returns the session's additional working directories (`--add-dir`), not just the launch directory — see mcp-and-plugins.md. Trivial: login-expiry warning; grey ⏸ badge in footer for Manual permission mode; [VSCode] Settings toggle to enable Remote Control for all sessions. ~25 bug fixes: background sessions (stale token, PATH inheritance on Windows, dropped `ANTHROPIC_BASE_URL`, crash-loop on deleted cwd), worktree-isolated subagents running in parent checkout, nested-repo worktree creation, `TaskStop`/`TaskOutput` not finding cross-agent-spawned agents, `effortLevel` settings.json changes ignored mid-session, `CLAUDE_CODE_DISABLE_MOUSE` opt-out ignored on attach, daemon auto-upgrade killing running sessions. (v2.1.203)
- **v2.1.202** (2026-07-06): Added a "Dynamic workflow size" option in `/config` to control how many agents a dynamic workflow spawns (see tools-and-scheduling.md `Workflow` tool row). Skills: re-invoking a skill whose rendered content is identical to what's already in context now shows a short "already loaded" note instead of appending a duplicate copy — before v2.1.202, every re-invocation appended the full instructions again even when nothing had changed (see skills.md). Remote Control: the mode dropdown on claude.ai/mobile now reports the local session's actual permission mode live, including mode changes made from the terminal — except `bypassPermissions`, which a session never reports; before v2.1.202 Remote Control sessions didn't report their mode at all (see modes-and-permissions.md). Also: `workflow.run_id`/`workflow.name` OTel attributes; `/review <pr>` reverted to fast single-pass review. Bug fixes: Ctrl+R history search crash, `/rename` on background sessions reverted on job restart, transient mTLS handshake failures during cert rotation, Remote Control "Unknown command" errors and dropped attachment captions, sign-in URL not clickable over SSH, `claude agents` chat-open failures, workflow script unicode-quote corruption, unbounded voice-dictation retry, slow session resume with many git worktrees, installer/updater failures on mid-connection drops; improved `/workflows` agent list layout and MCP error messages. (v2.1.202)
- **v2.1.113**: security hardening -- `sandbox.network.deniedDomains` setting; `Bash(find:*)` no longer auto-approves `find -exec`/`-delete`; deny rules now match commands wrapped in `env`/`sudo`/`watch`/`ionice`/`setsid`; macOS `/private/{etc,var,tmp,home}` treated as dangerous.
- **v2.1.114** (2026-04-18): `/less-permission-prompts` renamed to `/fewer-permission-prompts`; permission-dialog crash fix when teammate requested tool permission.
- **v2.1.116** (2026-04-20): `/resume` 67% faster; `/reload-plugins` auto-installs missing plugin deps; agent `hooks:` frontmatter fires via `--agent`.
- **v2.1.117**: `CLAUDE_CODE_FORK_SUBAGENT=1` enables forked subagents on external/non-Anthropic builds.
- **v2.1.118** (2026-04-23): new `mcp_tool` hook type; hooks `"$defaults"` in auto mode rules; `/usage`; custom themes at `~/.claude/themes/`; `DISABLE_UPDATES` env var; Vim visual mode (v, V).
- **v2.1.119** (2026-04-23): `PostToolUse`/`PostToolUseFailure` hooks include `duration_ms`; `prUrlTemplate` setting; `--from-pr` accepts GitLab MRs/Bitbucket PRs/GHE; `/config` (no args) persists to `~/.claude/settings.json` — `/config key=value` inline form added in v2.1.181; new index pages: `admin-setup`, `auto-mode-config`.
- **v2.1.120** (2026-04-28): Windows no longer requires Git Bash to launch CC (PowerShell default); `claude ultrareview [target]` non-interactive; `${CLAUDE_EFFORT}` in skills.
- **v2.1.121** (2026-04-28): `alwaysLoad` MCP option; `PostToolBatch` hook event; `PostToolUse` hooks can replace tool output for any tool; `claude plugin prune` removes orphaned deps; type-to-filter in `/skills`.
- **v2.1.122** (2026-04-28): `ANTHROPIC_BEDROCK_SERVICE_TIER` env var; PR URL search in `/resume`; `/mcp` duplicate server detection.
- **v2.1.123** (2026-04-29): OAuth 401 retry-loop fix when `CLAUDE_CODE_DISABLE_EXPERIMENTAL_BETAS=1`.
- **v2.1.126** (2026-05-01): `claude project purge` deletes all CC state for a project; `bypassPermissions` now bypasses protected paths (rm -rf / and ~ still prompt); OAuth improvements for WSL2/SSH/containers; auto-mode spinner turns red when classifier stalls; image auto-downscale >2000px on paste.
- **v2.1.128** (2026-05-04): bare `/color` picks random session color; `/mcp` shows tool count per connected server; `--plugin-dir` accepts `.zip` archives; `--channels` works with console (API key) auth; `workspace` is reserved MCP server name; MCP reconnect no longer floods chat with tool lists; auto mode classifier errors include helpful hints. New `allowedMcpServers`/`deniedMcpServers` managed settings for MCP server allowlists/denylists.
- **v2.1.129** (2026-05-06): `--plugin-url <url>` CLI flag installs plugin from URL `.zip` archive; `skillOverrides` setting: `off` (disable all auto-invocation), `user-invocable-only` (only user-invocable skills visible to model), or `name-only` (model sees names but not descriptions); Ctrl+R history picker now searches all projects by default (Ctrl+S narrows to current project). (v2.1.129)
- **v2.1.131** (2026-05-06): bug fixes only — VS Code extension activation on Windows; Mantle endpoint auth header fix.
- **v2.1.132** (2026-05-06): `CLAUDE_CODE_SESSION_ID` env var injected into Bash tool subprocess environment (scripts can correlate with session). `CLAUDE_CODE_DISABLE_ALTERNATE_SCREEN=1` opts out of fullscreen rendering. Bug fixes: SIGINT graceful shutdown, `--resume` emoji truncation, `--permission-mode` ignored on plan-mode resume, fullscreen blank after sleep/wake, stdio MCP server memory leak.
- **v2.1.133** (2026-05-07): Active effort level now in all hook JSON inputs (`effort.level` field; `$CLAUDE_EFFORT` env var for command hooks). `worktree.baseRef` setting (`fresh`/`head`). Bug fixes: subagents not discovering project/user/plugin skills; permission rules broken on mapped network drives; Remote Control stop/interrupt not fully canceling CLI; `/effort` affecting concurrent sessions.
- **v2.1.136** (2026-05-08): `autoMode.hard_deny` — unconditional auto mode blocking rules; user intent and `allow` exceptions cannot override. `CLAUDE_CODE_ENABLE_FEEDBACK_SURVEY_FOR_OTEL` env var for enterprise OTel deployments. Bug fixes: MCP servers disappeared after `/clear`; MCP OAuth refresh tokens lost on concurrent refresh; login loops from concurrent credential writes; extended thinking API errors with redacted blocks; `--resume`/`--continue` with underscores in project paths; WSL2 image paste via PowerShell fallback.
- **v2.1.137** (2026-05-09): VSCode extension activation fix on Windows.
- **v2.1.138** (2026-05-09): Internal fixes.
- **v2.1.139** (2026-05-11): Agent View research preview: `claude agents` opens unified session list showing all running CC sessions; `/background` detaches current session as background agent. `/goal` command: set completion condition, Claude works until met. `claude plugin details <name>`: component inventory + token cost projections. New tools: `PushNotification` (desktop + phone push via Remote Control; not on Bedrock/Vertex/Foundry) and `RemoteTrigger` (manage Routines on claude.ai, backs `/schedule`; requires Pro/Max/Team/Enterprise). Improved MCP OAuth handling, retry logic, env var passing.
- **v2.1.141** (2026-05-13): `terminalSequence: "<string>"` in hook JSON output emits a terminal escape sequence (desktop notifications, bell). `CLAUDE_CODE_PLUGIN_PREFER_HTTPS` env var for GitHub plugin cloning over HTTPS. `ANTHROPIC_WORKSPACE_ID` for workload identity federation. Enhanced `/feedback` shows last 24h/7-day sessions. "Summarize up to here" option in Rewind menu. 40+ bug fixes. (v2.1.141)
- **v2.1.140** (2026-05-12): Bug fixes — `subagent_type` matching now case/separator insensitive; `/goal` hang fix with certain hooks; background service startup fix on enterprise machines; tool validation fixes. (v2.1.140)
- **v2.1.142** (2026-05-14): `claude agents` dispatched session flags: `--add-dir`, `--settings`, `--mcp-config`, `--plugin-dir`, `--permission-mode`, `--model`, `--effort`, `--dangerously-skip-permissions`. Fast mode defaults to Opus 4.7 (`CLAUDE_CODE_OPUS_4_6_FAST_MODE_OVERRIDE=1` to pin 4.6). Plugins with root-level `SKILL.md` and no `skills/` subdirectory surface as skills. `MCP_TOOL_TIMEOUT` env var now properly raises per-request fetch timeout for remote servers (was capped at 60s). Bug fixes: background sessions + pre-existing worktrees, daemon exit after binary upgrade. (v2.1.142)
- **v2.1.143** (2026-05-15): Plugin dependency enforcement — `claude plugin disable` refuses when another enabled plugin depends on it. `worktree.bgIsolation: "none"` setting for direct working copy editing by background agents. Background session persistence: model and effort level preserved after idle wake; background agents now also preserve MCP configuration. PowerShell passes `-ExecutionPolicy Bypass` by default; enabled by default on Windows for Bedrock/Vertex/Foundry. Projected context costs shown per-turn in plugin marketplace. Bug fixes: corrupt `.credentials.json` hanging CLI startup, Esc/Ctrl+C not cancelling pending `/loop` wakeup, `/goal` evaluator firing while background shells run. (v2.1.143)
- **v2.1.152** (2026-05-27): `/code-review --fix` applies review findings to working tree after the review; `/simplify` now invokes `/code-review --fix` (no longer a plain alias). `disallowed-tools` frontmatter field in skills/slash commands removes tools from the model while that skill is active. `/reload-skills` command re-scans skill directories without restarting. New `MessageDisplay` hook event: transform or hide assistant message text before display. Auto mode no longer requires opt-in consent — cycling to auto via Shift+Tab activates immediately. (v2.1.152)
- **v2.1.151** (2026-05-24): Internal release (no user-facing changes). (v2.1.151)
- **v2.1.150** (2026-05-23): Internal infrastructure improvements (no user-facing changes). (v2.1.150)
- **v2.1.201** (2026-07-03): Claude Sonnet 5 sessions no longer inject harness reminders via a mid-conversation system role. Internal/bug-fix only. (v2.1.201)
- **v2.1.200** (2026-07-03): **Default permission mode formally named "Manual"** in the CLI, `claude --help`, VS Code, and JetBrains; config value stays `default`, and `manual` is now an accepted alias everywhere a mode value is set (flag, settings, subagent `permissionMode` frontmatter) — see modes-and-permissions.md. `AskUserQuestion` dialogs (and permission prompts, incl. plan approval) no longer auto-continue after 60s idle by default (v2.1.198/199 behavior via `CLAUDE_AFK_TIMEOUT_MS`) — opt back in via `/config`. Auto mode: new default blocks for tampering with security-guarding tests, deleting unnamed stateful resources Claude didn't create, repointing API/proxy/webhook/registry endpoints at an unfit host, retargeting git push destination without naming the remote, pushing secrets to a known-public repo, and cross-repo/org PRs or forks without naming the target; PR/issue bodies and commit messages now count as outbound content for the v2.1.198 sensitive-sharing block — see modes-and-permissions.md. Bug fixes: background session reliability (stalls, crashes, rate limiting, daemon crashes/stale `daemon.lock`), subagents cut off by rate limits no longer return empty results, control bytes no longer reach terminals from background-agent output, misleading voice-dictation "Voice connection failed" message, screen-reader/terminal-output-sync improvements, clearer install-script OOM messaging. (v2.1.200)
- **v2.1.199** (2026-07-02): Stacked skill invocations — `/skill-a /skill-b do XYZ` loads every named skill (up to 6) and passes the trailing text to each as arguments. SSL certificate errors now fail immediately with actionable guidance instead of hanging. Streaming responses preserve partial output with an incomplete-response notice instead of discarding it on drop. Subagents: a rate-limit or server-error cutoff now returns partial work to the parent instead of failing outright (foreground gets partial output + a cut-off note; background is marked failed with its last output included) — see agents.md. `SendMessage` now verifies a name still refers to the same agent reached earlier in the conversation before delivering. Bug fixes: Linux background-agent daemon self-destruction, macOS SSH background-agent "Could not switch to audit session" failure, transient 429s now auto-retry with backoff for subscribers. (v2.1.199)
- **v2.1.198** (2026-07-01): **Subagents now run in the background by default** (Claude still runs foreground when it needs the result immediately) — see agents.md. `Explore` now inherits the main conversation's model (capped at Opus on the Claude API) instead of always Haiku. `/agents` no longer opens the interactive wizard — ask Claude or edit `.claude/agents/` directly. New env vars: `CLAUDE_CODE_DISABLE_EXPLORE_PLAN_AGENTS=1` (drop built-in Explore/Plan), `CLAUDE_AGENT_SDK_DISABLE_BUILTIN_AGENTS=1` (drop all built-ins, SDK/non-interactive). Claude in Chrome reached general availability. Background agents now auto-commit and open a draft PR when they finish code work. Auto mode: `git commit --amend` also blocked when HEAD is already pushed (message-only reword still allowed); new default blocks for `/tmp` mass deletion by wildcard, unauthorized sensitive-data sharing, and sending keystrokes to Claude's own tmux pane; sandbox network verdicts now cached per host+port instead of re-classified every connection — see modes-and-permissions.md. Gateway failover improvements for Claude Platform on AWS. (v2.1.198)
- **v2.1.197** (2026-06-30): **Claude Sonnet 5** is now the default model with a native 1M-token context window. Promotional pricing: $2 input / $10 output per Mtok through August 31, 2026. Sonnet 5 now supported in auto mode on Amazon Bedrock, Google Cloud Vertex AI, and Microsoft Foundry (requires `CLAUDE_CODE_ENABLE_AUTO_MODE=1`). (v2.1.197)
- **v2.1.196** (2026-06-29): `/loop` scheduled fires now only execute skills Claude is allowed to auto-invoke; built-in commands, `disable-model-invocation: true` skills, `skillOverrides`-restricted skills, and MCP prompts are passed as plain text. New bundled Skill: `/design-sync` converts React design system for Claude Design (Anthropic API only). Organization default models support (admin-configurable default model org-wide). Readable default session names; clickable file attachments in chat; background job reliability improvements. (v2.1.196)
- **v2.1.195** (2026-06-26): **Auto mode safety expanded (doc addition)** — expanded classifier block/allow lists; see modes-and-permissions reference for full details. Bug fixes — hook matchers with hyphenated identifiers (e.g., `mcp__brave-search`, `code-reviewer`) now exact-match correctly. `CLAUDE_CODE_DISABLE_MOUSE_CLICKS` env var disables mouse click interactions in fullscreen while preserving wheel scrolling. Voice dictation, plugin permission prompt, agents list display, and Remote Control startup improvements. (v2.1.195)
- **v2.1.194** (internal): No user-facing changes. (v2.1.194)
- **v2.1.193** (2026-06-25): `autoMode.classifyAllShell` setting routes ALL Bash/PowerShell commands through the auto-mode classifier (previously reads and working-dir edits were auto-approved in auto mode). Auto-mode denial reasons now recorded in transcript, toast, and `/permissions` recent-denials list. OTel `claude_code.assistant_response` log event (redacted by default; `OTEL_LOG_ASSISTANT_RESPONSES` controls). Bash `!` live file-path autocomplete. MCP auth startup notice when servers need authentication (points to `/mcp`). Background agents: launch result no longer instructs "end your response" early. `headersHelper` MCP OAuth auto-reconnects on 401/403 errors. Bug fixes: stale UI after `/login`, phantom subagent spawning, agent panel hiding. (v2.1.193)
- **v2.1.191** (2026-06-24): **`/rewind` now works before `/clear`** — `/rewind` (and alias `/undo`) can restore the conversation state from before a `/clear` was run, not just undo the last turn. Fixed: scroll-position jumping during streaming; background agents resurrecting after being stopped; `/voice` improved error messaging for policy-disabled cases. (v2.1.191)
- **v2.1.190** (2026-06-24): Bug fixes and reliability improvements only. (v2.1.190)
- **v2.1.188/v2.1.189** (internal): No user-facing changes.
- **v2.1.187** (2026-06-23): `sandbox.credentials` setting — prevents sandboxed Bash commands from reading credential files and secret env vars (new security boundary for sandbox mode). Org-configured model restrictions now appear in the model picker and `/model` with "restricted by your organization's settings" messaging. Mouse click support for select menus in fullscreen mode. Bug fixes: `--resume` failing with "No conversation found"; `--json-schema` structured output looping after success; remote MCP tool calls hanging beyond 5 minutes; Korean/CJK text corruption; subagent/channel/agent tracking issues. (v2.1.187)
- **v2.1.186** (2026-06-22): `claude mcp login <name>`/`claude mcp logout <name>` — CLI MCP auth without opening `/mcp` menu; `--no-browser` stdin redirect for SSH sessions. `claude mcp get`/`claude mcp remove` now suggest typo corrections and truncate long server lists. `teammateMode: "iterm2"` — new agent-teams display mode for iTerm2 native split panes (requires `it2` CLI). `!` bash commands now trigger Claude to respond automatically — set `"respondToBashCommands": false` to opt out. Background subagents now surface permission prompts in the main session; press Esc to deny one call without stopping the subagent (previously auto-denied). Skills section added to `/plugin` Installed tab; status filter (`f`) in `/workflows` detail view. Bug fixes: streaming after machine sleep, subagent panel scroll, permission enforcement for named subagent spawns, Workflow schema validation loop, strikethrough rendering. (v2.1.186)
- **v2.1.185** (2026-06-20): Stream-stall hint messaging improved — detection timing and hint text updated. Bug fixes only. (v2.1.185)
- **v2.1.184** (internal): No user-facing changes. (v2.1.184)
- **v2.1.183** (June 19, 2026): **Auto mode safety expanded** — force push, direct push to `main`, and infrastructure destroy commands (e.g. `terraform destroy`, cloud `delete`/`destroy` CLI operations) now blocked by the classifier when not explicitly requested. **`Artifact` tool** — publishes an HTML or Markdown file as a private, interactive page on claude.ai, shareable within your organization (requires Team/Enterprise plan + `/login` auth; not on Bedrock/Vertex/Foundry; `disableArtifact` setting to turn off). Model deprecation warnings shown when selected model is deprecated or auto-updated. `/config --help` flag; Esc saves toggle settings. Bug fixes: API connection drops mid-response, thinking block display, MCP auth token exposure in transport headers, tmux panel integration, terminal rendering. (v2.1.183)
- **v2.1.182** (internal): No user-facing changes. (v2.1.182)
- **v2.1.181** (2026-06-17): `/config key=value` syntax sets any setting from the prompt inline (e.g. `/config model=claude-sonnet-4-5`; existing `/config` with no args still opens Settings UI). `sandbox.allowAppleEvents` setting enables Apple Events for sandboxed processes on macOS. `CLAUDE_CLIENT_PRESENCE_FILE` env var: when the named file exists, mobile push notifications are suppressed. Upgraded bundled Bun runtime to 1.4. Bug fixes: foreground subagents could spawn unbounded nested chains (now capped correctly); Write/Edit produced 0-byte or truncated files on network drives; macOS sandbox Apple Events permission; startup regression in fresh environments; corrupted `.claude.json` caused crashes; macOS TUI froze at session start. (v2.1.181)
- **v2.1.179/v2.1.180** (internal): No user-facing changes.
- **v2.1.178** (2026-06-15): **Permission rule syntax expanded** — `Tool(param:value)` now matches tool input parameter values with wildcard support (e.g., `Agent(model:opus)` blocks Opus-model subagents; `Agent(model:claude-3*)` matches all claude-3 variants). **Nested skills** in `.claude/skills/` subdirs now load contextually when Claude accesses files in that subdir; name clashes with parent-level skills shown as `<dir>:<name>` (e.g., `src:deploy`). **Auto mode**: subagent spawns now evaluated by classifier before launch (not just actions during the run). **Agent teams** (v2.1.178): `TeamCreate`/`TeamDelete` tools removed — spawning a teammate is now sufficient to start a team (no setup step). Team name auto-derived as `session-<first-8-chars-of-session-id>`; `team_name` input on Agent tool and `team_name` field in `TaskCreated`/`TaskCompleted`/`TeammateIdle` hook payloads are deprecated. Remote Control: better error messages with persistent indicators and connection status. Bug fixes: nested settings, skill loading, subagent transcripts, backend errors. (v2.1.178)
- **v2.1.177** (internal): No user-facing changes. (v2.1.177)
- **v2.1.176** (2026-06-12): Session titles now generated in conversation language (configured via existing `language` setting). Bedrock credential caching improved: credentials cached until `Expiration` field (not fixed 1h). Model enforcement: alias models can no longer redirect to blocked models; `/fast` refuses to toggle to unlisted models. Auto mode: falls back to best available Opus when Fable 5 unavailable. Permission rule pattern fixes (`Edit(src/**)`, `Read(~/.ssh/**)`, `Read(.env)` now match correctly). Linux sandbox: symlinked settings files no longer cause startup failures. Copy: fixed `/copy` and mouse-selection in tmux over SSH. Remote Control: model switching, disconnect notifications, re-login session handling. Background sessions: improved directory tracking, detachment, window management. (v2.1.176)
- **v2.1.175** (2026-06-12): Bug fixes only. (v2.1.175)
- **v2.1.174** (2026-06-12): Bug fixes only. (v2.1.174)
- **v2.1.173** (2026-06-11): Bug fixes only — Fable 5 model names with `[1m]` suffix now stripped automatically during normalization; spurious "sandbox dependencies missing" startup warning on Windows suppressed when sandbox is enabled in settings. (v2.1.173)
- **v2.1.172** (2026-06-10): **Sub-agents can now spawn their own sub-agents** (up to 5 levels deep) — the previous flat restriction is lifted; the `Agent` tool is now available within subagent context up to the depth limit. Amazon Bedrock auto-detects region from `~/.aws` config files when `AWS_REGION` is unset. Plugin marketplace search bar added in `/plugin`. OTEL: `model` attribute added to `claude_code.lines_of_code.count` metric. Bug fixes: sessions stuck on 1M context without usage credits; repeated "image could not be processed" errors with multiple images; agents view keeping sessions under "Working" for up to 30s; background agents reading wrong project settings; model ID context suffix doubled (e.g. `[1M][1m]`); `WebFetch(domain:*.example.com)` wildcard rules not matching subdomains; `/plugin` marketplace cursor lost after long lists; long-conversation redundant message normalization; idle CPU from `/goal` status chip re-rendering at 5Hz. (v2.1.172)
- **v2.1.171** (internal): No user-facing changes. (v2.1.171)
- **v2.1.170** (2026-06-09): **Fable 5** model support added to advisor tool — requires `v2.1.170+` and Fable 5 org access; use `--advisor fable` or `"advisorModel": "fable"` (not in `/advisor` picker). Bug fix: sessions launched from VS Code integrated terminal or shells inheriting CC env vars now properly save transcripts and appear in `--resume`. (v2.1.170)
- **v2.1.169** (2026-06-08): `--safe-mode` flag (and `CLAUDE_CODE_SAFE_MODE=1` env var) disables all customizations — CLAUDE.md, plugins, skills, hooks, MCP servers — for troubleshooting isolation. `/cd <path>` command moves session to new working directory without breaking prompt cache. `disableBundledSkills` setting hides bundled skills and built-in commands from the model. Bug fixes: enterprise managed MCP policies not enforced on reconnect/first session; ~30-50ms macOS UI stall for claude.ai users; `claude -p` hanging on Windows during skill scanning; Remote Control stuck on "reconnecting" after resume; `TaskCreate` malformed input auto-repair; CPU usage streaming/spinner; Vertex/Foundry 5-minute idle timeout restored. (v2.1.169)
- **v2.1.168** (2026-06-06): Bug fixes and reliability improvements only. (v2.1.168)
- **v2.1.167** (2026-06-06): Bug fixes and reliability improvements only. (v2.1.167)
- **v2.1.166** (2026-06-06): `fallbackModel` setting — list up to three fallback models tried in order when the primary model is overloaded or unavailable; CC also retries once on the fallback model for unexpected non-retryable errors. Glob pattern support in deny rule **tool-name position** (e.g. `Web*` denies WebFetch + WebSearch together; previously only argument positions supported globs). `MAX_THINKING_TOKENS=0`, `--thinking disabled`, and per-model thinking toggle now disable thinking even on models that think by default (e.g. Opus 4.8). Hardened cross-session messaging (security). Multiple bug fixes. (v2.1.166)
- **v2.1.165** (2026-06-05): Bug fixes and reliability improvements only. (v2.1.165)
- **v2.1.164** (2026-06-04): Internal release (no user-facing changes). (v2.1.164)
- **v2.1.163** (2026-06-04): `requiredMinimumVersion`/`requiredMaximumVersion` managed settings enforce min/max CC version org-wide. `/plugin list --enabled`/`--disabled` filter flags. Hooks: `additionalContext` in `hookSpecificOutput` now supported for Stop/SubagentStop events (expanding beyond SessionStart/Setup/UserPromptSubmit). Skills: `\$` before a digit escapes to a literal `$` (prevents `$1` being treated as argument substitution). Bug fixes: Bash commands, Windows compatibility, background sessions. (v2.1.163)
- **v2.1.162** (2026-06-03): `claude agents --json` now also shows what blocked sessions are waiting on. Explicitly listing Grep/Glob in allowed tools provides dedicated search tools rather than deferred discovery. `/effort` now confirms when your selection persists as the new default. Slash commands in autocomplete fill into prompt (press Enter to run). Remote Control shown as persistent footer pill with session link. Windsurf renamed to Devin Desktop in menus. Bug fixes: silent startup hangs with read-only config directories; WebFetch permission rules for preapproved domains; Windows path matching with backslashes and case variants; MCP timeout values below 1000ms floored to 1s; LSP workspaceSymbol operation; terminal width in agent status; agent view issues (image paste, message loss, session stalling). (v2.1.162)
- **v2.1.161** (2026-06-03): OTEL metrics: `OTEL_RESOURCE_ATTRIBUTES` values now labels on metric datapoints. Agent View: `claude agents` rows show `done/total` count when work is fanned out. Parallel Tool Calls: failed Bash commands no longer cancel other calls in the same batch. Bug fixes: managed-settings policies blocking third-party sessions; background subagent output corrupting `claude -p` stdout; Windows hooks failing with explicit bash invocation; OTel log events dropped before init. (v2.1.161)
- **v2.1.160** (2026-06-02): Protected files expanded — `.zshenv`, `.zlogin`, `.bash_login`, and `~/.config/git/` now prompt before writing in all modes except `bypassPermissions`. `acceptEdits` mode now also prompts before writing build-tool config files that grant code execution. Dynamic-workflow trigger keyword renamed from `workflow` to `ultracode` — use `/effort ultracode` (v2.1.154+ `workflow` keyword deprecated). Bug fixes: copy-on-select not writing to Windows clipboard on WSL; background sessions losing chat history on restore; auto mode classifier latency improvement. (v2.1.160)
- **v2.1.159** (2026-05-31): Internal infrastructure improvements (no user-facing changes). (v2.1.159)
- **v2.1.158** (2026-05-30): Auto mode now available on Bedrock, Vertex, and Foundry for Opus 4.7 and Opus 4.8 — opt in with `CLAUDE_CODE_ENABLE_AUTO_MODE=1`. (v2.1.158)
- **v2.1.157** (2026-05-29): Plugins in `.claude/skills/` directories now auto-load (no marketplace/install/`--plugin-dir` needed). `claude plugin init <name>` scaffolds a plugin in `~/.claude/skills/<name>/` with `.claude-plugin/plugin.json` and starter `SKILL.md`. `EnterWorktree` can now switch between Claude-managed worktrees mid-session. Bug fixes. (v2.1.157)
- **v2.1.149** (2026-05-22): `/usage` now shows per-category breakdown (skills, subagents, plugins, MCP servers). Multiple security fixes (PowerShell permission bypasses, sandbox issues). (v2.1.149)
- **v2.1.148** (2026-05-22): Hotfix — Bash tool returning exit code 127 on every command for some users (regression from v2.1.147). (v2.1.148)
- **v2.1.147** (2026-05-21): `/simplify` renamed to `/code-review` with optional effort-level argument (`/code-review`, `/code-review high`); `/simplify` alias retained temporarily. Pinned background sessions stay alive when idle. Windows PowerShell tool fix (winget/Store installs). MCP server pagination fix (resources/list, resources/templates/list, prompts/list). Fullscreen strobing fix in Windows Terminal background sessions. Auto-updater retry logic. Diff rendering performance for large files. (v2.1.147)
- **v2.1.145** (2026-05-19): `claude agents --json` lists live Claude sessions as JSON for scripting (tmux-resurrect, status bars, session pickers). New bundled skills: `/run` (launch app to verify a change works), `/verify` (build and run app to confirm code change without falling back to tests), `/run-skill-generator` (record build/launch recipe per project; run once, then `/run`+`/verify` use it). Security: fixed permission-prompt bypass for bare variable assignments to non-allowlisted env vars in Bash. OTel: `agent_id`/`parent_agent_id` attributes added to `claude_code.tool` spans. `/plugin` Discover/Browse screens now show plugin components before install. (v2.1.145)
- **v2.1.156** (2026-05-29): Bug fix — Opus 4.8 thinking block modification causing API errors when thinking blocks were altered. (v2.1.156)
- **v2.1.155** (2026-05-28): Internal release (no user-facing changes). (v2.1.155)
- **v2.1.154** (2026-05-28): **Opus 4.8** launch — defaults to high effort (`/effort xhigh`) for hardest tasks. **Dynamic workflows** (`workflows.md` page): ask Claude to create a workflow script that orchestrates work across tens to hundreds of background agents. `! <command>` in interactive sessions now runs shell commands as detached **background sessions** (frees terminal; session visible in Agent View). `/logout` now signs you out rather than opening a background session. Lean system prompt now default for all models except Haiku, Sonnet, and Opus 4.7 and earlier. Remote Control works on Bedrock, Vertex, Foundry, and with telemetry disabled. Plugins: `defaultEnabled: false` in `plugin.json` — plugin is disabled by default and must be explicitly enabled with `/plugin`. Discover tab pins suggested plugins for current directory. Bug fixes: auto mode, path handling, terminal rendering. (v2.1.154)
- **v2.1.153** (2026-05-28): `/model` now saves your selection as default for new sessions (behavioral change from v2.1.144 where it was current-session-only; pressing `d` in picker is no longer required to persist the default). Git LFS: new `skipLfs` option skips LFS downloads during clone/update (useful for repos with large LFS assets). Background agents now appear as "Claude Code" in macOS Privacy & Security; permission grants persist across upgrades. Bug fixes: MCP stateful servers without optional GET SSE stream reconnect-looping; custom API gateway potentially receiving user OAuth credentials. (v2.1.153)
- **v2.1.144** (2026-05-19): `/resume` now supports background sessions started via `claude --bg` or Agent View (marked `bg` in the session list). `/model` changes model for current session only; press `d` in the model picker to set the default for new sessions. `head`/`tail` Bash commands now satisfy the read-before-edit requirement (same as `cat`/`sed -n`). `/plugin` browse pane now shows when plugins were last updated. Fixed startup hang (up to 75s when `api.anthropic.com` unreachable, now times out after 15s). Bug fixes: terminal rendering corruption in long sessions, macOS background session crash in Full Disk Access-protected folders, MCP servers with paginated `tools/list` only returning first page. (v2.1.144)

### Doc-gap sweep (2026-07-12)

No new CC version this run (v2.1.207 still latest, unchanged since 2026-07-11). Full-page (non-condensed) fetches of 11 of 15 priority pages surfaced a wider-than-usual doc-gap sweep across 5 reference files (settings/commands/hooks pages were condensed by WebFetch again — no contradictions found in the condensed content):
- Sub-agents/Agent Teams: Explore thoroughness levels (quick/medium/very thorough); plugin `agents/` subfolders get a scoped identifier (`my-plugin:review:security`); manual `@agent-<name>`/`@agent-<scoped-name>` mention forms; agent-teams limitations previously uncaptured (no nested teams, no background subagents from in-process teammates, no session resumption for in-process teammates); a subagent definition's `skills`/`mcpServers` frontmatter is ignored when it's spawned as a teammate. See agents.md.
- Skills: `${CLAUDE_PROJECT_DIR}` substitution (v2.1.196) was missing from the substitutions list; skill entries can be symlinks to a directory elsewhere on disk. See skills.md.
- MCP: `notifications/roots/list_changed`; `CLAUDE_PROJECT_DIR` set in stdio server process env; `ENABLE_TOOL_SEARCH` env var modes (unset/true/auto:N/false) — a gap flagged and deferred since Run 79, resolved this run; MCP resources `@server:protocol://resource/path` mention syntax. See mcp-and-plugins.md.
- Memory: corrected two stale facts — `autoMemoryDirectory` IS read from project/local settings (gated by workspace-trust, not blocked outright as previously noted); `CLAUDE_CODE_NEW_INIT` takes `1`, not `true`. See memory-and-context.md.
- Tools reference: Bash timeout/output-length limits + shell-startup alias sourcing; Glob 100-file cap and `.gitignore`-exempt-by-default behavior; Grep respects `.gitignore` (contrast); WebFetch 15-min cache/preapproved-domains/redirect handling. See tools-and-scheduling.md.
- Permission-modes: re-confirmed the v2.1.207 doc-lag (live page still describes `CLAUDE_CODE_ENABLE_AUTO_MODE` as required on Bedrock/Vertex/Foundry) — no new edit, existing note already covers it.
- llms.txt oscillated again (alphabetical numbered ~165-page list, slight superset of the 2026-07-09/10 163-page style) — no confirmed new/removed priority-page slugs; every title cross-checked against the cumulative non-priority list from prior runs.

### Doc-gap sweep (2026-07-11)

New CC version this run (v2.1.207, see Version notes above). Additional cross-page items found:
- Sub-agents: model resolution (env var → per-invocation param → frontmatter → main conversation) also filters each candidate against the org's `availableModels` allowlist before use. See agents.md.
- MCP: stdio servers are now subject to the idle-timeout check as of v2.1.203 (30min window), correcting a stale "stdio exempt" note; per-server `timeout` floors the idle window. `Claude Browser` added to the reserved-MCP-server-name list in v2.1.205. See mcp-and-plugins.md.
- Settings/Skills: resolved the long-pending `skillListingMaxDescChars` vs `maxSkillDescriptionChars` naming flag (open since Run 80) — `skillListingMaxDescChars` confirmed authoritative after repeated consistent live fetches. See settings.md, skills.md.
- commands.txt and settings.txt fetches this run were condensed high-level summaries again (recurring WebFetch-summarization pattern) rather than exhaustive tables; no contradictions found against existing reference files. llms.txt repeated the categorical-by-topic-header style (distinct from the alphabetical-numbered style of the prior two runs) — no confirmed new/removed priority-page slugs; new `whats-new/2026-w27` and `whats-new/2026-w28` weekly digest pages noted, non-priority.

### Doc-gap sweep (2026-07-09)

New CC version this run (v2.1.205, see Version notes above). Full-page (non-summarized) fetches of hooks.txt and permission-modes.txt surfaced two doc-gap items beyond the version bump:
- Hooks: the live hooks reference documents exactly how the `if` field's Bash pattern matching works — leading env-var assignments are stripped, each subcommand of a compound command is checked separately, and commands inside `$()`/backticks are also checked. Not previously captured. See hooks.md.
- Permission modes: the live page's auto-mode block list has more nuance than the changelog-level summary we had for v2.1.203 — the default-branch push block is conditional on the push carrying sensitive content, concealed/misdescribed changes, ported-in content, or bypassing a requested review (a plain push to default isn't blocked on its own, refining the v2.1.183 "any direct push" entry); a new block covers sensitive local files (session transcripts, credential dot-folders, user-data exports) entering a commit/PR/gist/package-publish without naming both source and destination; the public-repo personal-data block gained a dotfiles-repo exception. See modes-and-permissions.md.
- commands.txt still describes `/doctor` as diagnosis-only with no `/checkup` alias — changelog-only doc lag (v2.1.205 shipped same day as this run); captured from the changelog directly, not from the commands page.
- llms.txt reverted to a comprehensive numbered list (163 pages, alphabetical-ish) — yet another format oscillation. No confirmed new/removed priority-page slugs. Two unfamiliar non-priority titles noted ("Champion kit", "Communications kit" — likely sales-enablement pages) for future confirmation; not acted on.

### Doc-gap sweep (2026-07-08)

Two new CC versions this run (v2.1.203, v2.1.204 — see Version notes above). Single doc-gap item found: the MCP page's `roots/list` description ("returns the directory Claude Code was launched from") hasn't caught up to the v2.1.203 changelog note that additional working directories are now included too; captured in mcp-and-plugins.md from the changelog since the page itself lags. `hooks.txt`/`hooks-guide.txt`/`commands.txt`/`tools-reference.txt` fetches this run were noticeably shorter/summarized than prior runs' comprehensive captures (likely a WebFetch small-model summarization artifact, not a doc content removal) — no facts found in the condensed content that contradict or extend existing reference files; flagged for a fuller re-fetch next run rather than acted on. `settings.md` gained `enableArtifact` (user-level override to re-enable Artifact tool, complements existing `disableArtifact`). llms.txt format oscillated again (narrative/curated style, distinct from the 2026-07-06/07 categorical style) — no confirmed new/removed priority-page slugs.

### Doc-gap sweep (2026-07-07)

New CC version (v2.1.202, see Version notes above). Additional cross-page doc-gap items found this run:
- Sub-agents: subagent scope resolution order was missing the managed-settings tier (ranks above the `--agents` CLI flag); `tools`/`disallowedTools` also accept `mcp__<server>`/`mcp__<server>__*` server-level patterns. See agents.md.
- Hooks: common JSON input includes `prompt_id` (UUID for the current user prompt, v2.1.196) for correlating multiple hook firings to the same turn. See hooks.md.
- Scheduled tasks: `ScheduleWakeup(stop: true)` explicitly ends a self-paced `/loop` (v2.1.202); a ~20min fallback wakeup now also guards against an iteration that neither reschedules nor stops. See tools-and-scheduling.md.
- Commands: `/review [PR]` (read-only GitHub PR review, distinct from `/code-review`) was undocumented in this file — added to Key Commands above.

### Doc-gap sweep (2026-07-06)

No new CC version (v2.1.201 still latest). Cross-page doc-gap sweep found several reference-only gaps (no product behavior change):
- Hooks: `Notification` matcher gains `agent_needs_input`/`agent_completed` (v2.1.198, Agent View only); `StopFailure` matcher gains `overloaded`/`oauth_org_not_allowed`/`model_not_found`; `Stop` hook 8-consecutive-block override cap + `stop_hook_active` field + `CLAUDE_CODE_STOP_HOOK_BLOCK_CAP`; `${CLAUDE_PLUGIN_DATA}` path placeholder (persistent plugin data dir, survives updates); HTTP hook `allowedEnvVars` field; command hook `shell: "bash"|"powershell"` field; multi-hook precedence (deny>defer>ask>allow; last-writer-wins for concurrent `updatedInput`); `PermissionRequest` doesn't fire in `-p` mode. See hooks.md.
- Subagents: tool-availability exceptions (`AskUserQuestion`/`EnterPlanMode`/`ExitPlanMode`/`ScheduleWakeup`/`WaitForMcpServers` unavailable even if listed); `/doctor` duplicate-name detection (v2.1.196); nested `.claude/agents/` closest-to-cwd resolution (v2.1.178); subagent `mcpServers` now subject to `--strict-mcp-config`/managed MCP policy (v2.1.153); fork reuses parent's prompt cache. See agents.md.
- Plugins/MCP: plugin MCP tool naming `mcp__plugin_<plugin>_<server>__<tool>`; `${CLAUDE_PLUGIN_DATA}`; `/reload-plugins --force` bypasses the prompt-cache-invalidation warning. See mcp-and-plugins.md.
- Permission modes: `permissions.disableAutoMode` managed lockout; `Cd(path)` permission rule for `/cd` (v2.1.169). See modes-and-permissions.md.
- Settings: possible rename/alias `skillListingMaxDescChars` vs. existing `maxSkillDescriptionChars` — flagged, unconfirmed.

### Doc-gap sweep (2026-07-05)

- **`/simplify` correction (v2.1.154)**: not merely `/code-review --fix` (as v2.1.152 made it) — became its own skill: 4 parallel review agents (reuse/simplify/efficiency/abstraction-level), applies fixes, does not hunt for correctness bugs. See skills.md.
- New tools found undocumented on live tools-reference: `ReportFindings` (v2.1.196, structures `/code-review` findings for rendering), `SendUserFile` (v2.1.196, sends a file to the user via Remote Control/cloud session with `display: render|attach`). See tools-and-scheduling.md.
- `Monitor` tool gained a WebSocket source (`ws` input: `url`/`protocols`) as an alternative to a polling script — connects directly to a server that pushes events. (v2.1.195)
- `.mcp.json` project-server approvals (`claude mcp list`/`get`) are only read from settings files not checked into the repo until the workspace-trust dialog is accepted — a cloned repo can't self-approve its own committed `enableAllProjectMcpServers`/`enabledMcpjsonServers`. (v2.1.196)
- `CLAUDE_CODE_MCP_TOOL_IDLE_TIMEOUT` env var: remote MCP tool calls (HTTP/SSE/WebSocket/claude.ai connector) with no response or progress notification for 5 minutes now abort instead of waiting for the full timeout; set to `0` to disable. (v2.1.187)
- Auto mode classifier trust boundary: a git remote added or repointed mid-session (`git remote add`/`set-url`) is not trusted for push-destination checks; before v2.1.200 mid-session remotes were trusted like pre-existing ones. (v2.1.200)
